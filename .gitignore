# Built landing page
# The generated project itself IS committed — the session container is
# ephemeral, so the source has to live in the repo. Only build output and
# installed packages are ignored.
site/node_modules/
site/.next/
site/out/
build/

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.production
.env.development

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Vercel
.vercel/

# Next.js (inside site/)
.next/
out/

# Python (ui-ux-pro-max skill scripts)
__pycache__/
*.py[cod]

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright
playwright-cli-screenshots/
*.png
!docs/**/*.png
# Site artwork is content, not a screenshot — keep it.
!site/public/**/*.png
